---
execute:
  echo: false
  warning: false
  error: false
format:
  html:
    toc: true
    standalone: false
    title-block-style: none
    ascii: true
  pdf:
    fig-dpi: 1000
    fig-pos: "H"
  docx:
    reference-doc: custom-reference-doc.docx
    fig-dpi: 1000
engine: knitr
bibliography: smelt.refs.bib
csl: apa.csl
nocite: |
  @*
---

```{r}
#| label: setup
#| echo: false
#| warning: false
#| message: false
#| output: false

library(tidyverse)
library(here)
library(kableExtra)
library(readxl)
library(glue)
library(viridis)
source("smelt_data_extraction.R")
source("smelt_functions.R")
source("smelt_vars.R")

# Setting up shortcuts to use for less than/equal to, greater than/equal to, and degree symbol that can be used inside
# R strings/glue()
GE  <- "$\\ge$"
LE  <- "$\\le$"
DEG <- "$^{\\circ}$"
```

## Weekly Assessment for Delta Operations on ESA and CESA-listed Osmerids

Last updated: *`r format(Sys.Date(), "%B %d, %Y")`*

### Executive Summary

```{r, results = 'asis', echo= FALSE}
cat(narrative_text)
```


### Operational and Regulatory Conditions

- See current Weekly Fish and Water Operations Outlook document.
- Additional information also available on the [SacPAS SMT page](https://www.cbr.washington.edu/sacramento/workgroups/delta_smelt.html).


### Delta smelt  

#### Biological

- **Delta smelt life stages detected in surveys:** `r ds_lifestages`

- **Abundance estimate:** `r paste0(abundance, " (95% CL: ", format(abundance_lcl,big.mark = ","), " to ", format(abundance_ucl,big.mark = ","), ")")` as of the week of `r abundance_date`
- **Releases:** A total of `r format(total_released, scientific = FALSE, big.mark = ",")` cultured Delta smelt have been released for WY 2026. The most recent release of `r format(last_release_count, scientific = FALSE, big.mark = ",")` fish occurred in `r format(last_release_location, scientific = FALSE, big.mark = ",")` on `r format(last_release_date, "%b %d, %Y")`. 
- **Delta smelt count:** `r ds_adults_count_wy` adult Delta smelt and `r ds_juveniles_count_wy` juvenile Delta smelt have been detected this water year. See @tbl-ds-recent-catch for recent detections, @fig-ds-map for spatial distribution, and @fig-ds-catch for temporal distribution.
- **Delta smelt salvage:** `r ds_salvage_count` Delta smelt has been salvaged, and the cumulative seasonal salvage is `r ds_cumsalvage`. 


**Notes**

- Since there are few recent detections of Delta smelt, estimation of distribution within the Delta is limited. 
- As mentioned in EDSM reporting, fork length ranges reported for Delta smelt and longfin smelt life stages are defined by permit reporting purposes and are not intended to delineate cohorts or distinguish from hatchery or wild origin. See @tbl-ds-recent-catch caption for fork-length ranges for age groups of Delta smelt. 
- See [SacPAS SMT Page](https://www.cbr.washington.edu/sacramento/workgroups/delta_smelt.html) for additional details on releases and detection in surveys and salvage.
- Historical salvage trends can be found at: [SacPAS Salvage Timing](https://www.cbr.washington.edu/sacramento/data/query_salvage_hrt.html)
  

```{r, echo = FALSE}
#| label: "tbl-ds-recent-catch"
#| tbl-cap: "Delta smelt detections in the last 2 weeks. Fork Length > 58mm = Adult, Fork Length 20-58mm = Juvenile, Fork Length < 20mm = Larva."
#| warning: false

kable(ds_recent_display)
```


```{r, echo = FALSE}
#| fig-width: 8
#| fig-height: 7
#| label: "fig-ds-map"
#| fig-alt: "Map showing catch location and relative count of Delta smelt captured across different regions in WY2026"
#| fig-cap: "Delta smelt distribution for WY 2026"
(map_detections_lj <- ggplot() + 
    geom_sf(data = WW_Delta, color = "darkslategray3") +
    geom_sf(data = R_EDSM_Strata_1718P1, aes(fill = Stratum), alpha = 0.4,inherit.aes = FALSE)+
    # geom_sf(data = releases_sf, shape = 23, size =3,  fill = "red", color = "black", inherit.aes = FALSE) + 
    geom_sf(data = ds_wy, aes(shape = source, size = total_catch),   inherit.aes = FALSE) +
    # geom_sf_text(data = sls_sf, mapping = aes(label = Station), size = 3, nudge_x = -0.012, nudge_y = 0.016) +
    annotation_north_arrow(location = "tl", which_north = "true",
                           pad_x = unit(.1, "in"), pad_y = unit(0.2, "in"),
                           style = north_arrow_fancy_orienteering) +
    annotation_scale(location = "bl", bar_cols = c("black", "white", "black", "white")) +
    scale_x_continuous(limits = c(-122.35, -121.3)) + 
    scale_y_continuous(limits = c(37.8, 38.6)) +
     scale_shape_manual(values = c(1, 15, 17, 4))+
    # scale_size(range = c(2, 2), breaks = c(1,1)) + 
    viridis::scale_fill_viridis(option = "turbo", discrete = TRUE) +
    guides(fill = guide_legend(nrow = 3, byrow = TRUE)) +
    theme_bw() +
    theme(axis.title.x = element_blank(),
          axis.title.y = element_blank(),
          axis.text = element_text(size = 10),
          axis.text.x = element_text(angle = 45, vjust = 0.5),
          legend.position = "top",
          legend.box = "vertical",
          legend.title = element_blank(),
          legend.text = element_text(size = 9)))
```

```{r, echo = FALSE, message = FALSE}
#| label: "tbl-catch-all"
#| tbl-cap: "Delta smelt water year totals by life stage"
#| warning: false
kable(
  ds_detail %>%
    filter(date > ymd("2025-10-01")) %>% 
    group_by(source, region, life_stage) %>%
    summarize(total = sum(catch)) %>%
    ungroup() %>%
    select(Survey = source, Region = region, `Life Stage` = life_stage, Total = total)
)
```

```{r, echo = FALSE}
#| label: "fig-ds-catch"
#| fig-cap: "Time series of Delta smelt catch, WY 2026"
#| fig-alt: "Column chart showing total catch of Delta smelt between Oct 1 and current day for multiple surveys."
ggplot(ds_wy) +
  geom_col(aes(date, total_catch, fill = source)) +
  labs(x = "Date", y = "Total Catch", fill = "Survey") + 
  scale_fill_viridis(option = "viridis", discrete = TRUE) + #was turbo- hard to see
  theme_bw()
```

#### Environmental

##### First Flush
```{r, echo = FALSE, results='asis'}
if (first_flush_status != "relevant") {
  cat("- Implemented 12/25/25-01/7/26\n") #change this text to show implementation dates
} else {
  cat(glue("
**Threshold**: 3-day avg FPT flow {GE} 25,000 cfs and 3-day avg FPT turbidity {GE} 50 FNU

- **FPT Flow (3-day average)**: {format(round(FPT_flow_avg), big.mark = ",")} cfs as of {format(env_last_date, '%b %d, %Y')}
- **FPT Turbidity (3-day average)**: {FPT_turb_avg} FNU as of {format(env_last_date, '%b %d, %Y')}
"))
}
```

```{r, echo = FALSE, eval = first_flush_status == "relevant", out.width = "85%"}
#| fig-alt: "Line graph showing average flow at Freeport on Y1 and average turbidity on Y2 for the previous 14 days."
knitr::include_graphics(here("smelt_figures/freeport_flow.png"))
```


#### Real-time Assessment Thresholds


##### Adult Delta smelt
 
```{r, echo = FALSE, results='asis'}
if (adult_ent_status != "relevant") {
  cat("- Adult Delta smelt action offramped on 02/12/2026\n
- No adult Delta smelt action was taken in WY26\n")
} else {
  cat(glue("
**Threshold**: If daily average JPF < 0 AND turbidity {GE} 12 FNU at OBI, HOL and OSJ

- **JPF**: {paste(formatC(JPF_1d, format = 'f', big.mark = ',', digits = 0), collapse = ', ')} cfs as of {format(JPF_1d_lastdate, '%b %d, %Y')}
- **OBI Turbidity**: {paste(OBI_turb, collapse = ', ')} FNU as of {format(env_last_date, '%b %d, %Y')}
- **HOL Turbidity**: {paste(HOL_turb, collapse = ', ')} FNU as of {format(env_last_date, '%b %d, %Y')}
- **OSJ Turbidity**: {paste(OSJ_turb, collapse = ', ')} FNU as of {format(env_last_date, '%b %d, %Y')}

<u>**Offramp Adult Protections** when RVB or SJW > 12{DEG} C for 3 consecutive days</u>

- **RVB (Rio Vista) temperature (3-day average)**: {paste(RVB_temp, collapse = ', ')}{DEG} C as of {format(env_last_date, '%b %d, %Y')}
- **SJW (Jersey Point) temperature (3-day average)**: {paste(SJW_temp, collapse = ', ')} {DEG} C as of {format(env_last_date, '%b %d, %Y')}
"))
}

# round to nearest 2 decimals only: {paste(sprintf('%.2f', JPF_7d), collapse = ', ')} 
```

```{r, echo = FALSE, eval = adult_ent_status == "relevant", out.width = "85%"}
#| fig-alt: "Line graph showing daily average turbidity over the last 14 days at 6 stations, represented by different line colors and symbols."
knitr::include_graphics(here("smelt_figures/freeport_turbidity.png"))
```

- See [Bay-Delta Live](https://www.baydeltalive.com/current_conditions/constituent-tracker-turbidity) for recent Delta-wide turbidity conditions.
<!--
- Let's link to wind plot on the other SacPAS SMT page. [Wind plot](https://github.com/BDO-Science/smt/blob/main/WindRose_Grace.R)
-->

##### Larval/juvenile Delta smelt

```{r, echo = FALSE, results='asis'}
if (larval_ent_status != "relevant") {
  cat("- Not active\n")
} else {
  cat(glue("
**Threshold**: After the onset of spawning, if JPF < 0 cfs AND turbidity is {GE} 12 FNU in the south Delta AND PTM modeling indicates the action would avoid {GE} 5% entrainment of Delta smelt population after 30 days

  - **12-station South Delta Turbidity**: The most recent average turbidity was {sd_turb} FNU as of {format(sd_turb_date, '%b %d, %Y')}
"))
}
```

#### Evaluation

**Delta smelt:**

1. After the start of entrainment management, is JPF < 0, is daily average turbidity ≥ 12 FNU in the OMR corridor (stations OBI, HOL, and OSJ)? Has the average water temperature at Jersey Point or Rio Vista not exceeded 53.6$^{\circ}$F (12$^{\circ}$C) for 3 consecutive days and/or has this action already been taken during WY 2026?

    `r ds_eval_1`

2. What is the evidence for the onset of Delta smelt spawning?

    `r ds_eval_2`

3. After the onset of spawning, have the following conditions occurred: JPF < 0 cfs, average turbidity is $\ge$ 12 FNU in the south Delta, and PTM modeling indicates the action would avoid $\ge$ 5% entrainment of the Delta smelt population at facilities after 30 days?

    `r ds_eval_3`
  

### Longfin smelt
#### Biological

- **Longfin smelt life stages detected in surveys:** `r lfs_lifestages`

- **Longfin smelt count:** `r lfs_adults_count_wy` adult, `r lfs_juveniles_count_wy` juvenile, and `r lfs_larvae_count_wy` larval longfin smelt have been detected this water year. See @tbl-lfs-recent-catch for recent detections, @fig-lfs-map for spatial distribution, and @fig-lfs-catch for temporal distribution.
- **Longfin smelt salvage:** `r lfs_salvage_count` longfin smelt have been salvaged, and the cumulative seasonal salvage is `r lfs_cumsalvage`. 
<!--
- Include plot of cumulative longfin salvage? [Salvage plot code](https://github.com/BDO-Science/smt/tree/main/daily_longfin_salvage)
-->

```{r, echo = FALSE}
#| label: "tbl-lfs-recent-catch"
#| tbl-cap: "Longfin smelt detections in the last 2 weeks. Fork Length > 84mm = Adult, Fork Length 20-84mm = Juvenile, Fork Length < 20mm = Larva."
#| warning: false

kable(lfs_recent_display)
```

```{r, echo = FALSE}
#| fig-width: 8
#| fig-height: 7
#| label: "fig-lfs-map"
#| fig-alt: "Map showing catch location and relative count oflongfin smelt captured across different regions in WY2026"
#| fig-cap: "Longfin Smelt Distribution for WY 2026"
(map_detections_lj <- ggplot() + 
    geom_sf(data = WW_Delta, color = "darkslategray3") +
    geom_sf(data = R_EDSM_Strata_1718P1, aes(fill = Stratum), alpha = 0.4,inherit.aes = FALSE)+
    # geom_sf(data = releases_sf, shape = 23, size =3,  fill = "red", color = "black", inherit.aes = FALSE) + 
    geom_sf(data = lfs_wy, aes(shape = source, size = total_catch),   inherit.aes = FALSE) + 
    # geom_sf_text(data = sls_sf, mapping = aes(label = Station), size = 3, nudge_x = -0.012, nudge_y = 0.016) +
    annotation_north_arrow(location = "tl", which_north = "true",
                           pad_x = unit(.1, "in"), pad_y = unit(0.2, "in"),
                           style = north_arrow_fancy_orienteering) +
    annotation_scale(location = "bl", bar_cols = c("black", "white", "black", "white")) +
    scale_x_continuous(limits = c(-122.5, -121.3)) +  #(limits = c(-122.35, -121.3))
    scale_y_continuous(limits = c(37.45, 38.6)) + #(limits = c(37.8, 38.6))
    scale_shape_manual(values = c(0, 1, 2, 3, 4,5, 6))+
    # scale_size(range = c(2, 2), breaks = c(1,1)) + 
    viridis::scale_fill_viridis(option = "turbo", discrete = TRUE) + 
    guides(fill = guide_legend(nrow = 3, byrow = TRUE)) +
    theme_bw() +
    theme(axis.title.x = element_blank(),
          axis.title.y = element_blank(),
          axis.text = element_text(size = 10),
          axis.text.x = element_text(angle = 45, vjust = 0.5),
          legend.position = "top",
          legend.box = "vertical",
          legend.title = element_blank(),
          legend.text = element_text(size = 9)))
```

```{r, echo = FALSE, message = FALSE}
#| label: "tbl-lfs-catch-all"
#| tbl-cap: "Longfin smelt water year totals by life stage"
#| warning: false
kable(
  lfs_detail %>% #could also do lfs_recent, but this is for wy totals
    filter(date > ymd("2025-10-01"),
           !is.na(life_stage)) %>% 
    group_by(source, region, life_stage) %>%
    summarize(total = sum(catch)) %>%
    ungroup() %>%
    select(Survey = source, Region = region, `Life Stage` = life_stage, Total = total)
)
```

```{r, echo = FALSE}
#| label: "fig-lfs-catch"
#| fig-alt: "Column chart showing total catch of longfin smelt between Oct 1 and current day for multiple surveys."
#| fig-cap: "Time series of longfin smelt catch, WY 2026"
ggplot(lfs_wy) +
  geom_col(aes(date, total_catch, fill = source)) +
  labs(x = "Date", y = "Total Catch", fill = "Survey") + 
  scale_fill_viridis(option = "viridis", discrete = TRUE) + #was turbo- hard to see
  theme_bw()
```


#### Real-time Assessment Thresholds

##### Start of Entrainment Management (Adult Longfin Smelt)
```{r, echo = FALSE, results='asis'}
if (first_flush_lfs_status != "relevant") {
  cat("- This action was not taken in WY26\n")
} else {
  cat(glue("
**Threshold**: If daily average JPF < 0 cfs AND salvage of adult longfin exceeds 5% of FMWT Index + 1

- **Daily average JPF**: {paste(formatC(JPF_1d, format = 'f', big.mark = ',', digits = 0), collapse = ', ')} cfs as of {format(JPF_1d_lastdate, '%b %d, %Y')}
- **Total LFS salvage**: {paste(lfs_salvage_count, collapse = ', ')} fish, with a cumulative salvage of {paste(lfs_salvage_count, collapse = ', ')}
"))
}

# round to nearest 2 decimals only: {paste(sprintf('%.2f', JPF_7d), collapse = ', ')}
# used to be {paste(JPF_1d, collapse = ', ')}
```


##### Adult longfin smelt

- **Threshold:** JPF < 0 cfs, annual loss is on a trajectory to exceed 5% of the adult population abundance, and reduced exports will reduce entrainment in the south Delta
  - Daily average JPF: `r formatC(JPF_1d, format = 'f', big.mark = ',', digits = 0)` cfs as of `r format(JPF_1d_lastdate, "%b %d, %Y")`
  - Adult abundance (Age 1+ LFS index): 2479.2 fish
      - 5% of abundance + 1: 125.0
  - Water year total adult longfin smelt salvage = 0
  

##### Larval/juvenile longfin smelt

- **Threshold:** JPF < 0 cfs AND population model demonstrates need to reduce entrainment to avoid population decline
  - Daily average JPF: `r formatC(JPF_1d, format = 'f', big.mark = ',', digits = 0)` cfs as of `r format(JPF_1d_lastdate, "%b %d, %Y")`
  
#### Evaluation

**Longfin smelt:**

1. If JPF < 0, what is the trajectory of annual loss of adult longfin smelt and is it likely to exceed 5% of the adult population estimate? Is South Delta entrainment expected to decrease due to a reduction in export pumping?

    `r lfs_eval_1`
  
2. For larval and juvenile longfin smelt, if JPF < 0 cfs, do particle tracking models show a moderate to high difference in particle fates across different OMRI scenarios? Does Zone of Influence modeling show moderate to high changes in hydrodynamic footprint across different OMRI scenarios? Are these effects anticipated to cause a population decline?

    `r lfs_eval_2`
  
3. Is there additional information or other analyses that should be considered in this evaluation?

    `r lfs_eval_3`


### End of smelt Entrainment Management
```{r, echo = FALSE, results='asis'}
if (end_of_season != "relevant") {
  cat("- Not relevant\n")
} else {
  cat(glue("
**Threshold**: CLC {GE} 25{DEG}C (77{DEG}F) for 3 consecutive days OR June 30

- Clifton Court Temperature: {paste(CLC_temp, collapse = ', ')}{DEG}C as of {format(env_last_date, '%b %d, %Y')}
"))
}
```

```{r, echo = FALSE, eval = end_of_season == "relevant", out.width = "85%"}
#| fig-alt: "Line chart showing water temperature at Clifton Court over the last 14 days, with a horizontal dashed line indicating the temperature threshold."
knitr::include_graphics(here("smelt_figures/clifton_court_temp.png"))
```


### References


